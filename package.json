{
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build:web": "npm run pages:build --workspace=web"
    },
    "devDependencies": {
        "wrangler": "^4.59.1"
    }
}